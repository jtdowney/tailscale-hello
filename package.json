{
  "name": "hello",
  "version": "1.0.0",
  "publish": false,
  "scripts": {
    "build": "tailwindcss build -m -i css/app.css -o static/app.css",
    "watch": "tailwindcss build -w -i css/app.css -o static/app.css"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.24"
  }
}
